RUN
{ALLOCATE CHANNEL d1 TYPE DISK;
  RESTORE SPFILE FROM AUTOBACKUP;
  RESTORE CONTROLFILE FROM AUTOBACKUP;
  SHUTDOWN ABORT;
  STARTUP MOUNT;
  RESTORE DATABASE;
  SHUTDOWN ABORT;
  STARTUP MOUNT;
  RECOVER DATABASE;
}